# Template for local dev (do not commit secrets).
#
# You can copy this file to:
# - `apps/web/.env.local` for Next.js dev
# - `.env.local` (repo root) for running scripts/cron locally

# Supabase
NEXT_PUBLIC_SUPABASE_URL=...
NEXT_PUBLIC_SUPABASE_ANON_KEY=...
SUPABASE_SERVICE_ROLE_KEY=...

# Email (server-only)
RESEND_API_KEY=...

# IPFS pinning (server-only)
PINATA_JWT=...

# Chain + Explorer
BASE_RPC_URL=https://mainnet.base.org
BASESCAN_API_KEY=...

# Cron security (server-only)
CRON_SECRET=...
